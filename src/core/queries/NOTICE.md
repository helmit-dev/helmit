# Attribution — tree-sitter tag queries

The `tree-sitter/*.scm` query files in this directory are copied verbatim from
the Aider project (https://github.com/Aider-AI/aider), directory
`aider/queries/tree-sitter-language-pack/`, licensed under the
**Apache License 2.0**. Copyright belongs to the Aider authors and the
upstream grammar authors credited there.

They define the `name.definition.*` / `name.reference.*` capture convention
used by HelmIt's `repo-map.sh` (deterministic, optional code-map layer —
ADR-009). Local modifications, if ever needed, must be noted per file.
